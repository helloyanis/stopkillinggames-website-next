import { createTheme } from "@mui/material"

const Theme = createTheme({
    palette: {
      primary: {
        main: '#2062a4',
        contrastText: "#fff" 
      },
    },
  });

export default Theme;